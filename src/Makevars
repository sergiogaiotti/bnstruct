# src/Makevars
PKG_CFLAGS = -Wpointer-sign $(SHLIB_OPENMP_CFLAGS)
PKG_LIBS   = $(SHLIB_OPENMP_CFLAGS)

# Fallback for compilers that don't define SHLIB_OPENMP_CFLAGS
ifeq ($(SHLIB_OPENMP_CFLAGS),)
  PKG_CFLAGS += -fopenmp
  PKG_LIBS   += -fopenmp
endif

